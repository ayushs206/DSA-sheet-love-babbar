#include <bits/stdc++.h>
using namespace std;

/*
int method1 () {
	string s, str2;
	cin >> s;

    for (int i=s.size()-1; i>=0; i--) {
	    str2 += s[i];
    }

	cout << str2;
}
*/

/*
int method2 () {
	string s;
	cin >> s;
	
	char temp;
	
	for (int i=0; i<s.size()/2; i++) {
		temp = s[i];
		s[i]=s[s.size()-1-i];
		s[s.size()-1-i]=temp;
	}

	cout << s;
}
*/

int main() {
	
}